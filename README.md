# تهاني زواج عبدالله

تجربة عربية بسيطة ومهيأة للجوال لضيوف زواج عبدالله يحيى الحِدريتي. يصل الضيف من رمز QR، يكتب اسمه وتهنئته، ثم يرسلها كنص عبر واتساب/الإيميل أو يولّد بطاقة PNG ويشاركها من جواله.

## نطاق النسخة الحالية

- نموذج قصير للاسم والتهنئة.
- إرسال نص مجهز مسبقًا عبر رابط واتساب أو `mailto:`.
- 3 تصاميم بطاقات ثابتة.
- توليد الصورة بالكامل داخل المتصفح باستخدام Canvas.
- مشاركة ملف الصورة عبر Web Share API عندما يدعم الجهاز ذلك.
- معاينة وتنزيل الصورة كحل بديل.
- RTL كامل وتصميم mobile-first.

لا توجد قاعدة بيانات أو خادم تطبيق أو حسابات أو لوحة تحكم أو تحليلات.

## التقنية

- Next.js (App Router + static export)
- React + TypeScript
- Tailwind CSS
- Browser Canvas + Web Share APIs
- نشر متوافق مع Vercel

تستخدم أوامر المشروع وضع Webpack المدمج في Next.js لأن بيئة التطوير الحالية تمنع المنفذ الداخلي الذي يستخدمه Turbopack أثناء معالجة Tailwind. لا يغيّر ذلك بنية الموقع الثابت أو طريقة نشره.

## التشغيل محليًا

المتطلبات: Node.js 20.9 أو أحدث وnpm.

```bash
npm install
npm run dev
```

ثم افتح `http://localhost:3000`.

أوامر التحقق:

```bash
npm run lint
npm run typecheck
npm test
npm run build
```

أو شغّلها كلها عبر `npm run check`.

## إعداد بيانات الزواج

كل البيانات العامة القابلة للتعديل موجودة في:

```text
src/config/wedding.ts
```

استبدل رقم واتساب والبريد التجريبيين قبل النشر. رقم واتساب يُكتب بالصيغة الدولية أرقامًا فقط، مثل `9665XXXXXXXX`، من دون `+` أو مسافات. لا يحتاج المشروع إلى `.env` لأن هذه المعلومات عامة وتظهر للمتصفح، ولا توجد أسرار في النسخة الحالية.

## خط ثمانية المحلي

توجد ملفات WOFF2 المطلوبة داخل:

```text
public/fonts/thmanyah/
```

الملفات المستخدمة حاليًا:

- `ThmanyahSans-Regular.woff2`
- `ThmanyahSans-Medium.woff2`
- `ThmanyahSans-Bold.woff2`
- `ThmanyahSerifDisplay-Bold.woff2`
- `ThmanyahSerifText-Regular.woff2`
- `ThmanyahSerifText-Medium.woff2`

راجع `public/fonts/thmanyah/README.md`. تستخدم الواجهة نسخة Sans، بينما تستخدم البطاقات Serif Display للاسم وSerif Text للتهنئة. يحمل مولّد Canvas العائلات نفسها قبل رسم الصورة.

تُعرّف الخطوط عبر `@font-face` محلي بدل CDN أو أسماء `next/font` المولّدة، لأن مولّد Canvas يحتاج أسماء عائلات ثابتة حتى تطابق صورة PNG المعاينة الظاهرة.

## البناء والنشر

```bash
npm run build
```

ينتج Next.js موقعًا ثابتًا في `out/`. على Vercel يكفي ربط المستودع واستخدام إعدادات Next.js الافتراضية؛ لا توجد متغيرات بيئة أو خدمات خارجية مطلوبة.

## القيود الحالية

- رسائل واتساب والإيميل لا تُرسل تلقائيًا؛ الضيف يؤكد الإرسال داخل التطبيق المختار.
- المشاركة المباشرة للملفات تعتمد على دعم الجهاز وHTTPS. يظهر زر تنزيل عند عدم الدعم.
- مشاركة iPhone Safari وAndroid Chrome يجب اختبارها على أجهزة فعلية قبل ليلة الزواج.
- لا تُحفظ التهاني ولا توجد لوحة عرض.
- بيانات التواصل الحالية placeholders إلى أن يستبدلها صاحب المشروع.
